import React from 'react';
import Cards from '../Cards/Cards';

const Courses = () => {
    return (
        <div>
            <h2>Courses Page</h2>
            <Cards></Cards>
        </div>
    );
};

export default Courses;