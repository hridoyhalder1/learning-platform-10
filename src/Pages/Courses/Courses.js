import React from 'react';

import Cards from '../Cards/Cards';

const Courses = () => {
    
    return (
        <div>
            
            <Cards></Cards>
        </div>
    );
};

export default Courses;